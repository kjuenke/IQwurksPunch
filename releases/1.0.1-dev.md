# IQwurksPunch 1.0.1 Development

**Development version:** 1.0.1-dev
**Development started:** August 19, 2026
**Milestone:** Fresh-install and migration compatibility maintenance release

## Status

Version 1.0.1 development is in progress.

This file is the version-matched development release note required by the
IQwurksPunch distribution-package planner and archive verifier. It is not the
final Version 1.0.1 release note.

## Starting Baseline

Version 1.0.1 development begins from the published Version 1.0.0 release:

```text
Release commit: a984eb2d9b2184e1ee6c8ba88cb86bd7be8e05ec
Release branch: release/1.0.0
Release tag: v1.0.0
```

The Version 1.0.0 production deployment remains unchanged while this
maintenance release is developed and validated.

## Maintenance Scope

Version 1.0.1 repairs the fresh-install migration path by:

- Reusing the migration manager when the migration runner prepares its history
  table
- Adding a missing `batch` column to legacy migration-history tables before
  batch queries are executed
- Making migration 001 compatible with a history table already created by the
  runner
- Making migration 004 safe when the `batch` column already exists
- Creating the missing `email_log` table during fresh installation
- Adding an idempotent migration that repairs installed databases without
  deleting or replacing existing email history
- Adding regression coverage for fresh installs, legacy migration tables, and
  existing email history
- Preserving the documented empty `plugins/` extension directory in source
  checkouts so distribution-package planning is not blocked

## Development Rules

During Version 1.0.1 development:

- The application version remains `1.0.1-dev`.
- Package previews and development archives require this file.
- The stable Version 1.0.0 release remains preserved on `main`,
  `release/1.0.0`, and `v1.0.0` until Version 1.0.1 is completed.
- The production Version 1.0.0 server is not changed during development.
- Final release promotion requires isolated Linux fresh-install and 1.0.0
  upgrade validation.

## Current Validation

The focused migration compatibility suite passes with PHP 8.5.9 and PHPUnit
12.5.31:

```text
3 tests
6 assertions
```

All changed PHP files pass syntax validation. The full 451-test suite was also
exercised on Windows: 427 tests passed, while 24 existing tests reported
Windows path, permission, process, or Linux runtime-directory incompatibilities.
Those environment-specific results do not replace the required Linux release
validation.

## Remaining Validation

Before stable promotion, Version 1.0.1 must complete:

- Full automated testing on the supported Linux deployment platform
- A clean fresh installation with all 18 migrations applied
- A controlled upgrade from an intact Version 1.0.0 database
- Database integrity and foreign-key checks
- Application route and dashboard checks
- Distribution-package preview, build, and independent verification
