{% extends "layouts/base.twig" %}

{% block content %}

<div class="d-flex flex-wrap justify-content-between align-items-center mb-4">

    <div>

        <h1 class="mb-1">
            Payroll Summary
        </h1>

        <div class="text-muted">
            Daily payroll totals using the configured company and labor rules.
        </div>

    </div>


    <div class="d-flex flex-wrap gap-2">

        <a
            href="/reports/payroll/weekly"
            class="btn btn-outline-secondary"
        >

            <i class="bi bi-calendar-week"></i>

            Weekly Payroll

        </a>


        <a
            href="/reports/payroll/export/csv"
            class="btn btn-success"
        >

            <i class="bi bi-file-earmark-spreadsheet"></i>

            Download CSV

        </a>


        <a
            href="/reports/payroll/export/pdf"
            class="btn btn-danger"
        >

            <i class="bi bi-file-earmark-pdf"></i>

            Download PDF

        </a>

    </div>

</div>


<div class="card shadow-sm">

    <div class="card-body">


        <div class="table-responsive">

            <table class="table table-striped align-middle">

                <thead>

                    <tr>

                        <th>
                            Employee
                        </th>

                        <th>
                            Employee #
                        </th>

                        <th>
                            Department
                        </th>

                        <th>
                            Gross
                        </th>

                        <th>
                            Meal Deduction
                        </th>

                        <th>
                            Unpaid Break
                        </th>

                        <th>
                            Regular
                        </th>

                        <th>
                            Daily OT
                        </th>

                        <th>
                            Double Time
                        </th>

                        <th>
                            Premium
                        </th>

                        <th>
                            Payable
                        </th>

                        <th>
                            Status
                        </th>

                    </tr>

                </thead>


                <tbody>


                    {% for employee in summary %}

                        {% set dailyOvertime =
                            employee.daily_overtime_hours
                            |default(
                                employee.overtime_hours
                                |default(0)
                            )
                        %}


                        {% set doubleTime =
                            employee.double_time_hours
                            |default(0)
                        %}


                        {% set premiumHours =
                            employee.premium_hours
                            |default(
                                dailyOvertime
                                +
                                doubleTime
                            )
                        %}


                        <tr>

                            <td>
                                {{ employee.name }}
                            </td>


                            <td>
                                {{ employee.employee_number }}
                            </td>


                            <td>
                                {{ employee.department ?: '—' }}
                            </td>


                            <td>
                                {{ employee.gross_hours|number_format(2) }}
                            </td>


                            <td>

                                {% set mealMinutes =
                                    employee.recorded_meal_minutes
                                    +
                                    employee.automatic_meal_deduction_minutes
                                %}

                                {{ mealMinutes }} min

                            </td>


                            <td>
                                {{ employee.unpaid_break_minutes }} min
                            </td>


                            <td>
                                {{ employee.regular_hours|number_format(2) }}
                            </td>


                            <td>
                                {{ dailyOvertime|number_format(2) }}
                            </td>


                            <td>
                                {{ doubleTime|number_format(2) }}
                            </td>


                            <td>
                                {{ premiumHours|number_format(2) }}
                            </td>


                            <td>

                                <strong>
                                    {{ employee.total_hours|number_format(2) }}
                                </strong>

                            </td>


                            <td>

                                {% if employee.complete %}

                                    <span class="badge text-bg-success">
                                        Complete
                                    </span>

                                {% else %}

                                    <span class="badge text-bg-warning">
                                        Needs Review
                                    </span>

                                {% endif %}

                            </td>

                        </tr>


                        {% if not employee.complete %}

                            <tr class="table-warning">

                                <td colspan="12">

                                    <strong>
                                        Payroll warnings:
                                    </strong>

                                    <ul class="mb-0 mt-2">

                                        {% for error in employee.errors %}

                                            <li>
                                                {{ error }}
                                            </li>

                                        {% endfor %}

                                    </ul>

                                </td>

                            </tr>

                        {% endif %}


                    {% else %}

                        <tr>

                            <td
                                colspan="12"
                                class="text-center text-muted"
                            >
                                No payroll data available.
                            </td>

                        </tr>

                    {% endfor %}


                </tbody>

            </table>

        </div>


        <div class="small text-muted mt-3">

            Premium hours include daily overtime and double-time hours.

            Weekly overtime is calculated in the Weekly Payroll Summary and
            Payroll Workspace.

        </div>


    </div>

</div>


{% endblock %}
